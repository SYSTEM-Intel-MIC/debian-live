#!/bin/sh
set -e

# live-build auto/config 会调用此脚本
# 此文件在 lb config 之前执行，可设置环境变量
export LB_DISTRIBUTION="bookworm"
export LB_ARCHITECTURE="amd64"
export LB_BOOTAPPEND_LIVE="boot=live components quiet splash"
export LB_LINUX_FLAVOURS="amd64"
