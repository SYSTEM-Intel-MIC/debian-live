name: Build Custom Debian Live ISO

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install live-build
        run: |
          sudo apt-get update
          sudo apt-get install -y live-build

      - name: Generate live-build config
        run: |
          sudo lb config \
            --distribution bookworm \
            --architectures amd64 \
            --linux-flavours amd64 \
            --bootappend-live "boot=live components splash" \
            --binary-images iso-hybrid \
            --source false

      - name: Override apt mirrors to Aliyun + Tsinghua
        run: |
          MIRROR="http://mirrors.aliyun.com/debian/"
          SECURITY="http://mirrors.aliyun.com/debian-security/"

          # lb/ 目录由 sudo lb config 创建，文件属 root，全部操作需要 sudo
          sudo mkdir -p lb/config/archives

          # apt 偏好
          sudo tee /etc/apt/preferences > /dev/null << 'PREF'
          Package: *
          Pin: release o=Debian,a=bookworm
          Pin-Priority: 1000
          PREF

          # 覆盖三阶段镜像源（chroot + live + binary）
          sudo tee lb/config/archives/mirrors.binary.chroot > /dev/null << 'EOF'
          deb http://mirrors.aliyun.com/debian/ bookworm main contrib non-free non-free-firmware
          deb http://mirrors.aliyun.com/debian-security/ bookworm-security main contrib non-free non-free-firmware
          deb http://mirrors.tuna.tsinghua.edu.cn/debian/ bookworm main contrib non-free non-free-firmware
          deb http://mirrors.tuna.tsinghua.edu.cn/debian-security/ bookworm-security main contrib non-free non-free-firmware
          EOF

          sudo tee lb/config/archives/mirrors.binary.chroot.chroot > /dev/null << 'EOF'
          deb http://mirrors.aliyun.com/debian/ bookworm main contrib non-free non-free-firmware
          deb http://mirrors.aliyun.com/debian-security/ bookworm-security main contrib non-free non-free-firmware
          deb http://mirrors.tuna.tsinghua.edu.cn/debian/ bookworm main contrib non-free non-free-firmware
          deb http://mirrors.tuna.tsinghua.edu.cn/debian-security/ bookworm-security main contrib non-free non-free-firmware
          EOF

          sudo tee lb/config/archives/mirrors.binary.live > /dev/null << 'EOF'
          deb http://mirrors.aliyun.com/debian/ bookworm main contrib non-free non-free-firmware
          deb http://mirrors.aliyun.com/debian-security/ bookworm-security main contrib non-free non-free-firmware
          deb http://mirrors.tuna.tsinghua.edu.cn/debian/ bookworm main contrib non-free non-free-firmware
          deb http://mirrors.tuna.tsinghua.edu.cn/debian-security/ bookworm-security main contrib non-free non-free-firmware
          EOF

          # 同步仓库的 archives 到 lb/ 目录
          sudo cp -r config/archives/* lb/config/archives/

      - name: Add SSH package list
        run: |
          mkdir -p config/package-lists
          echo "openssh-server" > config/package-lists/ssh.list.chroot

      - name: Copy repo config into live-build tree
        run: |
          # 仓库的 package-lists 也要 sudo 复制
          sudo cp -r config/package-lists lb/config/

      - name: Build the ISO
        run: |
          sudo lb build 2>&1 | tail -80
          ls -lh *.hybrid.iso 2>/dev/null || ls -lh *.iso 2>/dev/null || find . -name "*.iso" -ls

      - name: Find ISO artifact
        id: find-iso
        run: |
          ISO=$(find . -name "*.hybrid.iso" -o -name "*.iso" | grep -v "stage\|source" | head -1)
          echo "iso_path=$ISO" >> $GITHUB_OUTPUT
          echo "ISO: $ISO"

      - name: Upload ISO artifact
        uses: actions/upload-artifact@v4
        if: steps.find-iso.outputs.iso_path != ''
        with:
          name: custom-debian-live-amd64
          path: ${{ steps.find-iso.outputs.iso_path }}
          retention-days: 7
